FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY orchestrator_agent ./orchestrator_agent
ENV PYTHONPATH=/app
CMD ["python", "-m", "orchestrator_agent", "--host", "0.0.0.0", "--port", "10007"]
